# Conflict Resolution

Conflict commands:

```bash
ds memory conflicts list
ds memory conflicts show <conflict_id>
ds memory conflicts resolve <conflict_id> --winner <run_or_memory_id>
ds memory conflicts mark-disputed <memory_id>
ds memory lifecycle review
```

Lifecycle actions are append-only and preserve prior records.
